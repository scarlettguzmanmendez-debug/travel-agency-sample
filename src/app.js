const flights=[{from:'SCL',to:'LIM',date:'2025-09-01',price:180000}];document.getElementById('flightForm').addEventListener('submit',e=>{e.preventDefault();const from=document.getElementById('from').value.trim().toUpperCase();const to=document.getElementById('to').value.trim().toUpperCase();const date=document.getElementById('date').value;const ul=document.getElementById('results');ul.innerHTML='';flights.filter(f=>f.from===from&&f.to===to&&f.date===date).forEach(r=>{const li=document.createElement('li');li.textContent=`${r.from}→${r.to} ${r.date} $${r.price}`;ul.appendChild(li);});});